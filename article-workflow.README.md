# article-workflow.html — AI × 記事制作フロー

「テーマを決めてから1本の記事が完成するまで」を12ステップに整理した、1枚もの静的HTML資料。
21日間チャレンジ（マインド編／AI編 Day1〜14）の内容を、記事制作という一本道に再構成したもの。

## ファイル

| ファイル | 役割 |
|---|---|
| `article-workflow.html` | 本体。CSS・JSすべて内包した単一ファイル。サーバー不要でそのまま開ける |
| `article-workflow.README.md` | このファイル |

CSS・JSは自己完結しており `style.css` には依存しないが、`index.html` とは相互にリンクしている。

- `index.html` の「学習アウトプット」セクション（`#output`、修了証と一緒に働くメリットの間）から本ページへ
- 本ページのヘッダー右端「← ポートフォリオ」から `index.html` へ

セクションを増やしたことで背景色の交互パターンがずれるため、`#value`（一緒に働くメリット）の
クラスを `section alt` から `section` に変更している。`index.html` のセクション順を入れ替える際は
この交互パターンに注意すること。

## 構成

1. ヒーロー
2. この資料について（何を入れ、何を外したか）
3. 全ステップに共通する3原則
4. 全体像 — 7フェーズ / 12ステップのマップ
5. 12ステップ詳細（進捗バー付き）
6. 最終チェックリスト
7. 1本目の記録テンプレート
8. プロンプト早見表（自動生成）
9. 出典フッター

### 12ステップ

| STEP | 内容 | 区分 |
|---|---|---|
| 00 | 読者を決める | 必須 |
| 01 | テーマを決める | 必須 |
| 02 | 切り口を決める | 必須 |
| 03 | リサーチする | 必須 |
| 04 | タイトルを決める | 必須 |
| 05 | 見出し構成（設計図）を作る | 必須 |
| 06 | リード文を書く | 必須 |
| 07 | 本文の前半を書く | 必須 |
| 08 | 後半＋結論を書く | 必須 |
| 09 | 通し読みで流れを直す | 推奨 |
| 10 | 推敲する | 必須 |
| 11 | ファクトチェックする | 必須 |
| 12 | 最終確認して記録する | 必須 |

## 機能

- **ワンクリックコピー** — 全16個のプロンプトにコピーボタン。`navigator.clipboard` を使い、
  非セキュアコンテキスト（`file://` 等）では `execCommand` にフォールバックする
- **進捗の保存** — 各ステップと最終チェックのチェックボックスは `localStorage`
  （キー: `article-workflow-progress-v1`）に保存。外部送信は一切なし
- **プロンプト早見表の自動生成** — 本文中の `.prompt` ブロックを走査して一覧を生成するため、
  プロンプトを追加・削除しても早見表は自動で追従する
- **PDF化** — ブラウザの印刷（`@media print` 対応）。ナビ・コピーボタン・進捗バーは印刷されない
- レスポンシブ、`prefers-reduced-motion` 対応、インラインSVG favicon

## 編集するとき

### プロンプトを追加する

ステップ本文に以下のブロックを足すだけ。早見表とコピー機能は自動で有効になる。

```html
<div class="prompt">
  <div class="prompt-head">
    <span>プロンプト NN｜何をさせるか</span>
    <button class="copy-btn" type="button">コピー</button>
  </div>
  <pre>プロンプト本文。差し替え箇所は <mark>〇〇</mark> で囲む</pre>
</div>
```

`<pre>` の中身がそのままコピーされる（`<mark>` はコピー時に除去されず、テキストのみが入る）。

### ステップを追加する

`<article class="step" id="stepNN">` を複製し、末尾のチェックボックスの
`data-key` を一意な値にする。進捗バーの分母（`data-key="stepNN"` の個数）は自動で更新される。

### 配色を変える

`:root` のカスタムプロパティを書き換える。値はリポジトリ直下の
`design-system-modern.yaml`（`modern-professional` テーマ）に準拠。

| トークン | 値 | 用途 |
|---|---|---|
| `--navy` | `#0f172a` | 見出し帯・ヘッダー・プロンプト背景 |
| `--blue` | `#2563eb` | アクセント・リンク・コピーボタン |
| `--amber` | `#f59e0b` | 「必須」バッジ・差し替え箇所のハイライト |
| `--bg` | `#f8fafc` | 背景 |
| `--text` | `#1e293b` | 本文 |
| `--green` | `#10b981` | 完了状態・OK例 |
| `--red` | `#e11d48` | 注意・NG例 |

## 権利・公開について

本資料は「21日間チャレンジ マインド編／AI編 Day1〜Day14」（note・poten_coach）の内容に基づく
**個人の学習アウトプット**。プロンプト文および各原則は同教材に由来する。

**第三者の著作物を含むため、社外公開・再配布の前に権利者への確認が必要。**
本ページは `index.html`（公開ポートフォリオ）の「学習アウトプット」セクションからリンクされており、
サイトを公開すれば第三者からも到達できる状態にある。教材由来のプロンプト文をそのまま掲載しているため、
公開範囲については権利者の許諾を確認しておくことが望ましい。

## 出典の補足

元教材の Day9（マインド編・AI編）の出典URLは note のプレビューリンク
（`note.com/preview/...?prev_access_key=`）で、プレビューキーは失効する可能性がある。
Day1〜7 と同じ正規URL（`note.com/poten_coach/n/...`）への差し替えが望ましい。
