# CLAUDE.md

このリポジトリで作業する際のルール。

## 単独HTMLページ（Artifact／資料ページ）の文字サイズ

このプロジェクトでは、ポートフォリオ本体（`index.html`）以外に、学習アウトプットや資料をArtifact（`claude.ai/code/artifact/...`）として公開することがある（例：AI×記事制作フロー、マインド編実践ノート）。過去に文字サイズの調整で同じ失敗を2度繰り返したため、ルール化する。

### rem の基準は `body` ではなく `html`

CSSの `rem` は **`html`（ルート要素）のfont-size** を基準に計算される。`body{font-size:18px}` のように `body` に指定しても、子孫要素が `rem` を使っている限りその値は無視され、実際は `html` の既定値（通常16px）で計算され続ける。これに気づかず「拡大した」と誤認したことが過去に2回あった。

**文字サイズを変えるときは、必ず `html`（または `:root`）に `font-size` を指定する。**

```css
html{font-size:19px}
@media (max-width:640px){
  html{font-size:18px} /* スマホ側も別途明示。放置すると既定の16px基準に戻る */
}
body{font-size:1rem; ...} /* remで統一。pxを直書きしない */
```

### PC・スマホは別々に検証する

「スマホで見たら気にならない」は当てにならない。スマホブラウザは画面が小さい分、OS側の表示調整で相対的に大きく感じることがあり、実際のCSS計算値とは別問題。**PC幅とスマホ幅（640px以下）の両方で、実際に計算された `font-size` をヘッドレスブラウザで計測して確認する。** 目視のスクリーンショットだけで判断しない。

```js
// ページに一時的に注入して確認する
getComputedStyle(document.documentElement).fontSize   // html基準（最重要）
getComputedStyle(el).fontSize                          // 実際の要素で確認
```

### 補助テキストが本文より小さくなっていないか確認する

見出しや本文だけでなく、注釈・ラベル・補足説明（例：カードの中の「使うなら」のような小見出しと本文）が、意図せず本文より一段階小さいサイズになっていることがある。全体を拡大しても、こうした要素だけ相対的に小さいままだと「まだ小さい」という指摘が再発する。**サイズ調整のときは、ページ内の全 `font-size` を洗い出して、主要な本文サイズを下回っている箇所がないか確認する。**

### 作業手順（推奨）

1. `<style>` 内の `font-size:` を全部 grep して一覧化する
2. `html` に明示的な `font-size` を設定する（未設定なら既定16px扱いになっている）
3. PC幅・スマホ幅（640px以下）それぞれで `getComputedStyle` を使い、主要な要素（本文・見出し・補助テキスト）の実測値を確認する
4. ヘッドレスブラウザでスクリーンショットを撮り、レイアウト崩れ（はみ出し・重なり）がないか確認する
5. 修正後、必ずビフォー/アフターの実測px値を報告する（「大きくしました」ではなく「16px→18pxにしました」と数値で示す）
